/* ============================================
   DOROLAND Pixel Art Engine
   Only applies to newly generated SVG pixel art.
   Real photos (kit, character) are NOT touched.
   ============================================ */

/* No canvas pixelation — all pixel effects handled via CSS
   image-rendering: pixelated on .pixel-char-img and .pixel-icon only. */
